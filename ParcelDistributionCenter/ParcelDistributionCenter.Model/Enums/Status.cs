namespace ParcelDistributionCenter.Model.Enums
{
    public enum Status
    {
        In_preparation,
        Stored_by_sender,
        Stored_in_machine,
        In_delivery,
        Delivered,
    }
}