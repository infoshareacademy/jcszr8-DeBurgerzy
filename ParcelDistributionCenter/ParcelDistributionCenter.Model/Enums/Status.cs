namespace ParcelDistributionCenter.Model.Enums
{
    public enum Status
    {
        In_preparation,
        In_delivery,
        Stored_in_machine,
        Stored_by_sender,
        Delivered,
    }
}